<!-- Introduction: A brief, non-technical explanation of the component's purpose and the problem it solves.
Usage Example: A code block showing exactly how to import and use the component in a project.
Props/API Table: A clear table listing the component's properties, including their Name, Type, Default Value, and Description. Mark required props with an asterisk (*).
Design/Usage Guidelines: "Do's and Don'ts" for when to use this component versus another (e.g., "Use a Button for actions, use a Link for navigation"). -->


# [Component Name]

Short description of what this component does and when to use it.

## Usage

```jsx
import { ComponentName } from './components';

function App() {
  return <ComponentName property="value" />;
}
