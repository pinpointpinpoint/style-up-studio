import type {Metadata} from 'next'

export const metadata: Metadata = {
    title: 'All',
    alternates: {
        canonical: '/work/all',
    },
}

export default function AllWorkRoute() {
    return null
}
